#include <bits/stdc++.h>
#define TURNFALSE true
#define int long long
#define inf 0x3f3f3f3f3f3f3f3f
#define endl '\n'
using namespace std;
signed main() {
    if(!TURNFALSE){
        ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    }
    else{
        freopen("test.in", "r", stdin);
        freopen("test.out", "w", stdout); 
    }
    
    //read in n
    //create a vector of size n+1 and use a for loop to read in the data
    int evens = 0, odds = 0;
    //for loop on the vector to find out how many evens and how many odds
    //do math here
    //if evens == odds, cout (evens + odds)
    //else if evens > odds, cout (evens + odds + 1)
    //else if evens < odds{
    //  diff = odds - evens;
    //  if diff%3 == 0, cout (evens + odds + diff / 3)
    //  if diff%3 == 1, cout (evens + odds + diff/3 - 1)
    //  if diff%3 == 2, cout (evens + odds + diff/3 + 1)
    //}
  
}